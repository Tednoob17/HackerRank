#!/bin/bash

sort -t $'\t' -k 2 -nr 

# -t : delimiter the $ is used for precise that is the a special characters
# -k : specify field 
# -n ; for precise the numerical numbr 
# -r for make a reverse of output 
